# Queue Dependency Requests — CONTRACT-QUEUE-001 consumer review

- Date: `2026-08-04`
- Requester/owner: `A2-QUEUE`
- Historical reviewed contract: `CONTRACT-QUEUE-001@1.0.0-draft.1`
- Active corrected contract: `CONTRACT-QUEUE-001@1.0.0-draft.2`
- Status: `DRAFT / A2_QUEUE_REVIEW_PASSED / AFFECTED_OWNER_REREVIEW_PENDING`
- API dependency: `CONTRACT-API-001@0.1.0-draft.1`
- Rule: no disposition authorizes runtime, provider selection, or DB-003.

All ten draft.1 responses were received. Six were constrained acceptances, not
unconditional acceptance; two specification conflicts and two rejections
blocked merge. The affected owners MUST re-review draft.2 at the new
A2-QUEUE-reviewed head. Silence remains non-acceptance.

At that review point, A2-QUEUE's independent draft.2 review found two final defects: the exact
Workflow repaired-candidate sequence was omitted and the constrained responses
were not fully enumerated. Both records were corrected; final A2-QUEUE review,
affected-owner re-review, current-main reconciliation, and commit/push were
then pending.

A2-QUEUE's renewed review accepted the exact Workflow sequence and confirmed
the 39-row table was structurally valid. The source-fidelity defects in 17
Auth, Integration, and Evaluation rows were corrected, and A2-QUEUE's final
source review passed. `QUEUE-004-C3-C2-CONSTRAINT-REGISTER-SOURCE-FIDELITY-CORRECTION`
is `COMPLETE / ACCEPTED_BY_A2_QUEUE` in reviewed correction commit
`16a396f03b2de7b1bf2c8a0380e6463fb7f42773`.

Current-main freshness reconciliation is `COMPLETE / FRESHNESS_RECONCILED / NO_QUEUE_SEMANTIC_CONFLICT_FOUND`:
normal merge reconciles latest `origin/main` `9a28d72eb08303b6701bf7db6df622006991196a` under task `QUEUE-004-C4B-LATEST-MAIN-FRESHNESS-RECONCILIATION-001`. Queue contract SHA-256 remains unchanged (`106ef0e5c4a58a6010d55f890ac852f7869e041baca963298e4af436e5aa5327`).
Auth PR #29 (`CONTRACT-AUTH-001@1.1.0-draft.1`) merged into `origin/main` at `9a28d72eb08303b6701bf7db6df622006991196a`; observed and verified that it does not alter Queue semantics. Focused draft.2 re-review is `PENDING / NOT_BEGUN / NEXT_STEP` for A2-AGENT-WORKFLOW,
A2-DATABASE, A2-EVIDENCE, A2-EXECUTION, A2-SECURITY, A2-AUTH, A2-DEPLOYMENT,
A2-INTEGRATION, and A2-EVALUATION. A2-BACKEND is not reopened because draft.2
changed no Backend/API-owned normative boundary.

PR #24 remains `OPEN / DRAFT / NOT_READY / NOT_MERGED`; provider remains
`UNSELECTED`; runtime remains `NOT_IMPLEMENTED / NOT_TESTED / NOT_AUTHORIZED`;
DB-003 remains `NOT_STARTED / UNAUTHORIZED`. Draft.2 remains unaccepted and
not implementation-ready.

| Request / reviewer | Draft.1 disposition | Constraint or blocking finding preserved in draft.2 | Future closure input |
|---|---|---|---|
| `QUEUE-REVIEW-BACKEND-001` / `A2-BACKEND` | `ACCEPTED_WITH_CONSTRAINTS` | Preserve API `202`, durable request/publication separation, idempotency, producer, and request/correlation boundaries. | API-boundary compatibility plus authorized implementation evidence. |
| `QUEUE-REVIEW-WORKFLOW-001` / `A2-AGENT-WORKFLOW` | `SPECIFICATION_CONFLICT` | Workflow owns run/step-occurrence attempt identity, repair allowance, semantic lifecycle/races/terminality, result-slot acceptance, and checkpoint authority; Queue transport cannot redefine them. | Affected-owner re-review of corrections 1–4 and 6–8. |
| `QUEUE-REVIEW-DATABASE-001` / `A2-DATABASE` | `ACCEPTED_WITH_CONSTRAINTS` | Database exclusively owns physical persistence, transactions, constraints, indexes, locking, compare-and-set, fencing enforcement, models, and migrations; no schema or DB-003 authorization. | Separately authorized physical design and implementation evidence. |
| `QUEUE-REVIEW-EVIDENCE-001` / `A2-EVIDENCE` | `SPECIFICATION_CONFLICT` | Evidence owns identity/reference issuance, semantic finalization, completeness, provenance, integrity meaning, compatibility, deletion, canonical content, and duplicate/conflict classification; it is not joint physical-persistence owner. | Affected-owner re-review of corrections 5 and 6. |
| `QUEUE-REVIEW-EXECUTION-001` / `A2-EXECUTION` | `REJECTED_WITH_REASON` | Execution owns worker/runtime behavior and facts, cancellation observation, bounded cleanup, heartbeat/renewal inputs/response observation, and producer-result production/serialization. | Affected-owner re-review plus future runtime and compatibility evidence. |
| `QUEUE-REVIEW-SECURITY-001` / `A2-SECURITY` | `ACCEPTED_WITH_CONSTRAINTS` | Security retains field/disclosure/trust approval, canonicalization and integrity algorithm approval, key policy, access/identity/secret boundaries, and required Security events. | External Security policy and future implementation evidence. |
| `QUEUE-REVIEW-AUTH-001` / `A2-AUTH` | `ACCEPTED_WITH_CONSTRAINTS` | Preserve current-authorization revalidation, separate service/actor identities, freshness, least privilege, and administrative authorization boundaries. | External Auth policy, compatibility review, and implementation evidence. |
| `QUEUE-REVIEW-DEPLOYMENT-001` / `A2-DEPLOYMENT` | `REJECTED_WITH_REASON` | Deployment owns runtime transport, clock behavior, operational cadence/configuration, provider mapping, retry/backoff/jitter/timeouts/capacity, adapter isolation, and operational capability—not semantic classification. | Affected-owner re-review plus future configuration, capability evidence, and measurements. |
| `QUEUE-REVIEW-INTEGRATION-001` / `A2-INTEGRATION` | `ACCEPTED_WITH_CONSTRAINTS` | Preserve provider-neutral common conformance, separately defined canonicalizations, mixed-version compatibility, rollback, and affected-consumer review after semantic change. | Compatibility matrix, conformance evidence, and rollout/rollback gate input. |
| `QUEUE-REVIEW-EVALUATION-001` / `A2-EVALUATION` | `ACCEPTED_WITH_CONSTRAINTS` | Implementation evidence remains separate from measurement and release gates; operational signals do not prove semantic success. | Measurement provenance, thresholds, capacity/reliability/latency, and release-gate input. |

## Accepted consumer-constraint register

These 39 entries preserve the six `ACCEPTED_WITH_CONSTRAINTS` responses. None
is unconditional acceptance or current implementation/release approval.

| Constraint ID | Responding manager | Exact affected sections / requirements | Constraint | Rationale | Compatibility impact | Required evidence | Closure condition | Contract-merge effect | Implementation-acceptance effect | Release effect |
|---|---|---|---|---|---|---|---|---|---|---|
| `BACKEND-Q-001` — Durable request/publication boundary | `A2-BACKEND` | Section 3; `QUEUE-REQ-017`, `QUEUE-REQ-018`, `QUEUE-REQ-025` | Physical transaction, outbox, or approved equivalent remains a separately authorized A2-DATABASE design decision. | API durable acceptance and Queue publication are separate facts. | Changing API `202` or durable-request/publication meaning requires Backend review. | Crash-window, duplicate-publication, and reconciliation tests. | Accepted Backend, Database, and Queue implementation contracts plus evidence. | `NO` | `YES` | `YES` |
| `BACKEND-Q-002` — API duplicate response | `A2-BACKEND` | Section 3.4; `QUEUE-REQ-003` | The HTTP response for an API duplicate, including possible `200`, `202`, or `409`, remains Backend-owned and is not selected by Queue. | Queue idempotency cannot redefine an HTTP response contract. | Changing duplicate-response behavior requires Backend/API review. | Backend idempotency and duplicate-response fixtures. | Versioned Backend implementation decision and tests. | `NO` | `YES` | `YES` |
| `BACKEND-Q-003` — Auth and Security policy | `A2-BACKEND` | Sections 5, 12, 16; `QUEUE-REQ-016` | Authorization, integrity, redaction, bounded-value, and disclosure policies remain Auth/Security-owned. | Backend-to-Queue publication must apply owner policy without Queue inventing it. | Any field, authorization, integrity, or disclosure semantic change requires Auth/Security and affected Backend review. | Positive/negative authorization, redaction, bounds, integrity, and disclosure fixtures. | Versioned Auth/Security policy plus conforming Backend/Queue implementation evidence. | `NO` | `YES` | `YES` |
| `BACKEND-Q-004` — Adapter and release boundary | `A2-BACKEND` | `QUEUE-REQ-026` | Provider choice, capacity, rollout, and adapter conformance remain Deployment, Integration, and Evaluation matters. | Backend acceptance cannot select a provider or substitute for adapter/release evidence. | Provider or adapter semantic changes require affected compatibility review. | Common adapter conformance, capacity, rollout, rollback, and release measurements. | Deployment/Integration/Evaluation acceptance under separately authorized implementation. | `NO` | `YES` | `YES` |
| `DATABASE-Q-001` — Database transaction scope | `A2-DATABASE` | Sections 3.2, 7; `QUEUE-REQ-007`, `QUEUE-REQ-017`, `QUEUE-REQ-024`, `QUEUE-REQ-025` | Database exclusively owns physical persistence; required accepted effects may use one Database transaction where applicable, while external Evidence/artefact bytes remain outside it. | Commit-before-ack requires an owner-defined atomic scope without making Evidence bytes Database transaction members. | Changing physical scope is an implementation decision; changing commit-before-ack or ownership requires affected contract review. | Crash-before/after-commit, partial-effect rejection, ack-loss, and external-reference tests. | Separately authorized Database design accepted with Workflow/Evidence/Queue contracts and evidence. | `NO` | `YES` | `YES` |
| `DATABASE-Q-002` — Outbox and inbox design | `A2-DATABASE` | Sections 3.2, 7; `QUEUE-REQ-017`, `QUEUE-REQ-018`, `QUEUE-REQ-019`, `QUEUE-REQ-025` | Producer crash-window protection and durable consumer deduplication are both required; one combined mechanism is acceptable only when both capabilities are proven. | Publication recovery and duplicate-consumer convergence close different failure windows. | Replacing the mechanism is compatible only when both guarantees and logical boundaries remain unchanged. | Commit/publish crash, relay restart, duplicate delivery, consumer restart, ack-loss, and convergence tests. | A2-DATABASE-approved outbox/inbox or equivalent design plus complete evidence. | `NO` | `YES` | `YES` |
| `DATABASE-Q-003` — Identity persistence | `A2-DATABASE` | Sections 3.2, 4, 7; `QUEUE-REQ-002`, `QUEUE-REQ-003`, `QUEUE-REQ-004`, `QUEUE-REQ-006`, `QUEUE-REQ-017`, `QUEUE-REQ-018`, `QUEUE-REQ-024` | Physical identity representation preserves typed semantic/message/publication/delivery/claim/attempt/result/Evidence separation; provider receipts remain operational metadata. | Persistence must not collapse semantic authority into provider or transport identifiers. | Identity collapse, authority change, or provider-receipt promotion is breaking and reopens affected review. | Uniqueness, cross-type rejection, stable-binding, duplicate-receipt, and provider-metadata fixtures. | Accepted physical schema/mapping under separate Database authorization with conformance evidence. | `NO` | `YES` | `YES` |
| `DATABASE-Q-004` — Fencing and concurrency | `A2-DATABASE` | Sections 6, 7, 9; `QUEUE-REQ-008`, `QUEUE-REQ-009`, `QUEUE-REQ-013`, `QUEUE-REQ-019`, `QUEUE-REQ-024` | Monotonic fencing or equivalent stale-writer rejection is required; process-local locks, time alone, worker assertion, last-write-wins, and provider receipts are insufficient by themselves. | Concurrent/replaced workers must not commit competing authoritative effects. | Weakening stale-writer rejection or accepted-effect atomicity is breaking. | Concurrent claim, replacement, expiry, stale commit, result, cancellation, and ack race tests. | A2-DATABASE-approved locking/CAS/fencing design with fault-injection evidence. | `NO` | `YES` | `YES` |
| `DATABASE-Q-005` — Retention and deletion | `A2-DATABASE` | Sections 10, 12, 16; `QUEUE-REQ-011`, `QUEUE-REQ-019`, `QUEUE-REQ-021` | Physical deletion/tombstone enforcement remains Database-owned; retention durations remain external owner policy and Queue cannot recreate deleted Evidence. | Replay and re-drive must respect durable deletion barriers without inventing retention. | Weakening deletion barriers or cross-category retention separation is breaking. | Expiry, tombstone, deletion, replay/re-drive, retention-category, and recreation-rejection tests. | Approved owner retention policies plus separately authorized Database enforcement and evidence. | `NO` | `YES` | `YES` |
| `DATABASE-Q-006` — Current-main reconciliation | `A2-DATABASE` | Entire reviewed contract and PR #24 against current `main` | PR #24 must be reconciled with current main before readiness. | Database assumptions and neighboring contracts may have changed since the reviewed head. | Any reconciliation change reopens affected review. | Fresh-head diff, conflict resolution, matrix/version checks, and affected regression evidence. | A2-QUEUE records successful current-main reconciliation and renewed affected review before PR readiness. | `YES — PRE_MERGE_REQUIREMENT` | `NO` | `NO` |
| `SECURITY-Q-001` — Sensitive envelope fields | `A2-SECURITY` | Sections 5, 12; `QUEUE-REQ-016`, `QUEUE-REQ-020`, `QUEUE-REQ-026` | Security approves sensitive/security-relevant field membership, disclosure, redaction, and trust treatment. | Allowlisting cannot establish safe disclosure by itself. | Field or disclosure-boundary changes require Security review. | Allowed/denied field, pre-serialization redaction, leak, and unknown-field fixtures. | Versioned Security field/disclosure policy plus conforming implementation evidence. | `NO — unless Security boundary wording changes` | `YES` | `YES` |
| `SECURITY-Q-002` — Payload limits | `A2-SECURITY` | Sections 5, 16; `QUEUE-REQ-016`, `QUEUE-REQ-026` | Payload, field, collection, and reference limits remain approved configuration informed by Security and capacity evidence. | Unbounded input creates availability and disclosure risk. | Limit changes are operationally compatible only when required-field semantics and safety do not weaken. | Boundary, oversize, cardinality, truncation-rejection, and capacity tests. | Approved configured limits with Security and Evaluation evidence. | `NO — unless Security boundary wording changes` | `YES` | `YES` |
| `SECURITY-Q-003` — Authorization freshness | `A2-SECURITY` | Sections 5.1, 9, 10, 12, 16; `QUEUE-REQ-006`, `QUEUE-REQ-011`, `QUEUE-REQ-016`, `QUEUE-REQ-020` | Authorization-freshness intervals and re-evaluation policy remain Auth/Security-owned external policy. | Stored authorization is provenance, not current authority. | Weakening revalidation boundaries is breaking; interval changes require policy review. | Expired/revoked/stale authorization, replay, re-drive, and protected-effect negative fixtures. | Approved Auth/Security freshness policy and implementation evidence. | `NO — unless Security boundary wording changes` | `YES` | `YES` |
| `SECURITY-Q-004` — Canonicalization | `A2-SECURITY` | Sections 7, 11, 15; `QUEUE-REQ-004`, `QUEUE-REQ-016`, `QUEUE-REQ-019`, `QUEUE-REQ-024`, `QUEUE-REQ-026` | Security approves versioned Queue-envelope and Execution-result canonicalization algorithms; Evidence canonicalization remains separately Evidence-owned. | Deterministic comparison must not collapse distinct owner semantics. | Algorithm/version or scope changes require Security and affected compatibility review. | Cross-implementation vectors, unknown-version rejection, mutable-field exclusion, and owner-separation tests. | Approved algorithms/versions and passing conformance vectors. | `NO — unless Security boundary wording changes` | `YES` | `YES` |
| `SECURITY-Q-005` — Integrity algorithms | `A2-SECURITY` | Sections 11, 16; `QUEUE-REQ-004`, `QUEUE-REQ-016`, `QUEUE-REQ-020`, `QUEUE-REQ-024`, `QUEUE-REQ-026` | Digest/MAC/signature choices, scopes, and authenticity claims remain external Security policy. | An unkeyed checksum proves consistency, not producer authenticity. | Algorithm/scope changes require Security and compatibility review. | Valid, tampered, substituted, wrong-scope, unknown-algorithm, and downgrade fixtures. | Approved integrity policy and interoperable verification evidence. | `NO — unless Security boundary wording changes` | `YES` | `YES` |
| `SECURITY-Q-006` — Key custody | `A2-SECURITY` | Sections 11, 12, 16; `QUEUE-REQ-016`, `QUEUE-REQ-020`, `QUEUE-REQ-026` | Key generation, custody, injection, rotation, revocation, and verifier access remain Security-owned. | Queue envelopes must not carry or redefine credential/key authority. | Key-policy or trust-root changes require Security review and rollout compatibility. | No-key-in-payload scans, injection-boundary, rotation, revocation, wrong-key, and least-privilege tests. | Approved key policy and operational custody/rotation evidence. | `NO — unless Security boundary wording changes` | `YES` | `YES` |
| `SECURITY-Q-007` — Security events | `A2-SECURITY` | Sections 5.1, 7, 10–12; `QUEUE-REQ-004`, `QUEUE-REQ-011`, `QUEUE-REQ-016`, `QUEUE-REQ-019`, `QUEUE-REQ-020`, `QUEUE-REQ-024`, `QUEUE-REQ-026` | Event schema, severity, disclosure, routing, and retention for required Security events remain Security-owned. | Fail-closed outcomes require attributable evidence without leaking prohibited content. | Removing events/attribution or increasing disclosure requires Security review and may be breaking. | Missing-event, attribution, redaction, tamper, stale-worker, conflict, replay, and unauthorized-admin fixtures. | Approved event contract plus durable, secret-free emission evidence. | `NO — unless Security boundary wording changes` | `YES` | `YES` |
| `SECURITY-Q-008` — Dead-letter disclosure | `A2-SECURITY` | Sections 10, 12; `QUEUE-REQ-011`, `QUEUE-REQ-020`, `QUEUE-REQ-021`, `QUEUE-REQ-026` | Dead-letter inspection/export disclosure and redaction policy remain Security-owned and action-specific. | Dead-letter metadata may retain sensitive provenance and cannot bypass current disclosure policy. | Disclosure expansion or action-boundary collapse requires Security/Auth review. | Least-privilege inspect/export, re-redaction, prohibited-content, and unauthorized-action tests. | Approved disclosure policy with administrative-path evidence. | `NO — unless Security boundary wording changes` | `YES` | `YES` |
| `SECURITY-Q-009` — Dead-letter retention | `A2-SECURITY` | Sections 10, 12, 16; `QUEUE-REQ-011`, `QUEUE-REQ-021` | Dead-letter retention remains external Security/legal policy and separately configured. | Queue transport retention cannot silently set Evidence, artefact, audit, or semantic retention. | Retention-policy weakening or category collapse requires owner review. | Expiry, deletion, category-separation, tombstone, and replay-after-expiry tests. | Approved retention policy and enforcement evidence. | `NO — unless Security boundary wording changes` | `YES` | `YES` |
| `SECURITY-Q-010` — Poison bounds | `A2-SECURITY` | Sections 8, 10, 16; `QUEUE-REQ-010`, `QUEUE-REQ-011` | Poison thresholds, retry bounds, and re-evaluation eligibility remain approved configuration/policy; Security rejection is not ordinary retry by default. | Bounds prevent denial-of-service while preserving semantic failure ownership. | Relabelling Security/application failure as poison or weakening boundedness is breaking. | Exhaustion, classification, malicious payload, Security rejection, and no-infinite-retry tests. | Approved bounds/policy with fault and capacity evidence. | `NO — unless Security boundary wording changes` | `YES` | `YES` |
| `SECURITY-Q-011` — Adapter Security parity | `A2-SECURITY` | Sections 5, 11, 12, 14; `QUEUE-REQ-016`, `QUEUE-REQ-020`, `QUEUE-REQ-022`, `QUEUE-REQ-026` | Every local/test and production adapter preserves validation, redaction, integrity, authorization, least privilege, encryption/access mapping, and secret isolation. | Provider mapping or test shortcuts must not weaken Security controls. | Any adapter control divergence is incompatible and requires review. | Common conformance plus provider-specific encryption, identity, secret-injection, isolation, and negative Security tests. | Security accepts adapter capability mapping and evidence. | `NO — unless Security boundary wording changes` | `YES` | `YES` |
| `SECURITY-Q-012` — Release Security acceptance | `A2-SECURITY` | Sections 12, 14–16; `QUEUE-REQ-020`, `QUEUE-REQ-026` | Release requires Security acceptance of configured controls, findings, operational signals, and residual risk; contract review alone is insufficient. | Normative wording does not prove deployed control effectiveness. | Control/configuration changes reopen Security release assessment. | Final Security test report, event/alert evidence, access review, key/secret review, and remediation disposition. | Explicit Security release acceptance for the measured implementation. | `NO — unless Security boundary wording changes` | `YES` | `YES` |
| `AUTH-Q-001` — Canonical actor resolution | `A2-AUTH` | Sections 4, 5; `QUEUE-REQ-002`, `QUEUE-REQ-006`, `QUEUE-REQ-020`, `QUEUE-REQ-026` | Actor references resolve canonically to `HUMAN_USER`, `GITHUB_APP_INSTALLATION`, `SYSTEM_SERVICE`, or `UNAUTHENTICATED`. Worker, operator, scheduler, webhook source, Queue producer, and administrator are roles/source categories and MUST NOT silently become new canonical actor types. | Stable attribution and policy evaluation require one canonical actor kind without role/type confusion. | Actor-type, role-mapping, or resolution changes require Auth and affected contract review. | Typed actor-reference schema; actor-confusion rejection fixtures; positive and negative fixtures for all four canonical types. | Versioned Auth actor-resolution contract and all required fixtures pass. | `NO — unless Auth semantics change` | `YES` | `YES` |
| `AUTH-Q-002` — Exact authorization-resource binding | `A2-AUTH` | Section 4 `authorization_context_reference`; Section 5 requester and authorization fields; `QUEUE-REQ-003`, `QUEUE-REQ-006`, `QUEUE-REQ-008`, `QUEUE-REQ-014`, `QUEUE-REQ-019`, `QUEUE-REQ-025` | Context binds, where applicable, actor, requested action, internal installation identity, internal repository identity, repository revision/revision constraint, semantic request, policy version, decision timestamp, and expiry/revalidation boundary. Mutable owner/name, user login, email, display name, or branch label MUST NOT become authorization keys. | Reuse or mutable labels across actor/install/repository/action/revision boundaries is privilege escalation. | Binding vocabulary, internal identity, revision, or resource-scope changes require Auth review. | Authorization-context schema; cross-repository substitution tests; cross-installation substitution tests; wrong-action, stale-context, and wrong-revision tests. | Auth-approved context schema and complete positive/negative binding suite pass. | `NO — unless Auth semantics change` | `YES` | `YES` |
| `AUTH-Q-003` — Worker service-principal model | `A2-AUTH` | Section 4 `worker_service_reference`; Section 5 claim fields; `QUEUE-REQ-008`, `QUEUE-REQ-009`, `QUEUE-REQ-026` | Implementation distinguishes stable service principal, service role, ephemeral service instance, credential version/reference, execution attempt, and Queue delivery. Credential rotation preserves historical attribution. | Restart, replacement, and rotation must preserve principal attribution without transferring stale claim authority. | Principal/role/instance/credential/attempt/delivery collapse or lost historical attribution requires Auth review. | Service-principal schema; credential-rotation fixture; revoked-worker credential test; principal-versus-instance confusion tests. | Versioned service-principal model and all rotation/revocation/confusion fixtures pass. | `NO — unless Auth semantics change` | `YES` | `YES` |
| `AUTH-Q-004` — Authorization freshness | `A2-AUTH` | Sections 9, 10, 12, 16; `QUEUE-REQ-005`, `QUEUE-REQ-006`, `QUEUE-REQ-008`, `QUEUE-REQ-011`, `QUEUE-REQ-014`, `QUEUE-REQ-015`, `QUEUE-REQ-019`, `QUEUE-REQ-026` | Future revalidation matrix covers publication, claim, repository checkout/access, protected execution effects, Evidence access, result acceptance, publication side effects, dead-letter inspection/export/deletion, redrive, and checkpoint recovery. Enqueue-time authorization is not automatically sufficient for a later protected operation. | Redelivery, replay, elapsed time, or stored context cannot preserve revoked or stale authority. | Weakening or omitting a revalidation boundary requires Auth/Security review and is breaking. | Approved freshness matrix; expiry race tests; revocation race tests; stale-policy and redelivery/replay tests. | Auth approves the complete freshness matrix and every race/replay fixture passes. | `NO — unless Auth semantics change` | `YES` | `YES` |
| `AUTH-Q-005` — Cancellation and recovery action vocabulary | `A2-AUTH` | Section 3.6; Section 4 `cancellation_actor_reference`; Section 5 cancellation fields; Section 9; `QUEUE-REQ-014`, `QUEUE-REQ-015` | Future Auth design separately defines authority for cancellation request, cancellation reversal, resume, regeneration, and checkpoint recovery. Workflow retains transition legality and terminal outcomes. | A generic cancellation/recovery permission must not authorize a different action or choose Workflow outcome. | Action addition/merging, target-scope change, or ownership change requires Auth/Workflow review. | Auth action matrix; Workflow acknowledgement; cross-action and wrong-target negative fixtures. | Auth and Workflow approve the action matrix and all negative fixtures pass. | `NO — unless Auth semantics change` | `YES` | `YES` |
| `AUTH-Q-006` — Dead-letter administrative authority | `A2-AUTH` | Section 10; `QUEUE-REQ-011`, `QUEUE-REQ-021` | Inspection, export, deletion, and redrive remain separately authorized actions. General Queue administration access MUST NOT authorize all four. | Authority for one dead-letter operation must not imply another. | Administrative action/scope changes or general-access expansion require Auth/Security review. | Least-privilege administrative authority matrix; cross-action denial fixtures; wrong-actor/resource and stale-authorization fixtures. | Auth/Security approve the authority matrix and every denial fixture passes. | `NO — unless Auth semantics change` | `YES` | `YES` |
| `AUTH-Q-007` — Auth runtime remains absent | `A2-AUTH` | `REVIEW-WIDE IMPLEMENTATION BOUNDARY; NO ADDITIONAL SECTION OR QUEUE-REQ MAPPING ASSERTED BY THE SOURCE REVIEW` | Auth runtime remains absent for authenticated Backend context, worker authentication, current authorization revalidation, repository authorization, credential rotation, cancellation authorization, replay authorization, and dead-letter administrative authorization. | A documentation contract cannot imply deployed Auth enforcement. | Any Auth runtime or semantic addition requires separate authorization and review. | Separately authorized Auth runtime implementation and tests. | Separately authorized Auth implementation is accepted by Auth/Security/Integration with tests. | `NO — unless Auth semantics change` | `YES` | `YES` |
| `INT-QUEUE-001` — Reusable adapter semantic-conformance suite | `A2-INTEGRATION` | Sections 14, 15; `QUEUE-REQ-022`, `QUEUE-REQ-026` | One reusable provider-neutral semantic suite validates every authorized adapter; provider-specific tests are additive only. | Shared semantics require one comparable oracle across adapters. | Suite/adapter semantic divergence is incompatible and reopens Integration review. | Deterministic ordering, duplicate-delivery, and concurrent-delivery controls; virtual/controllable time; crash/restart and acknowledgement-loss injection; renewal-request and renewal-response loss; lease-loss and stale-worker injection; poison/dead-letter injection; replay/re-drive controls; adapter capability manifest; exact contract/schema versions; clean-checkout execution; bounded secret-free artefacts. | The same approved semantic suite passes against every authorized adapter with declared capability and limitation evidence. | `NO` | `YES` | `YES` |
| `INT-QUEUE-002` — Cross-contract compatibility declaration | `A2-INTEGRATION` | Section 15; `QUEUE-REQ-002`, `QUEUE-REQ-015`, `QUEUE-REQ-016`, `QUEUE-REQ-020`, `QUEUE-REQ-026` | Declaration covers Queue contract, Queue envelope schema, canonicalization, API contract, Workflow event/lifecycle contract, Database schema revision, Evidence reference, Execution result/checkpoint, Auth actor-context/authorization-policy, Security classification/integrity-policy, and Deployment adapter-capability versions. | Undeclared owner-version combinations invite silent identity, compatibility, or semantic drift. | Any listed version, required field, or owner boundary change reopens affected review. | Supported-version matrix; unsupported-version rejection fixtures; mixed producer/consumer fixtures; optional-field compatibility fixtures; rollback behavior; existing-work version pinning; proof adapters do not drop required metadata. | Every required owner policy/version is available and the mixed-version suite passes. | `NO — provided no unresolved normative conflict is introduced` | `YES` | `YES` |
| `INT-QUEUE-003` — Duplicate, concurrency and atomicity validation | `A2-INTEGRATION` | Sections 2, 6, 7; `QUEUE-REQ-003`, `QUEUE-REQ-004`, `QUEUE-REQ-005`, `QUEUE-REQ-007`, `QUEUE-REQ-013`, `QUEUE-REQ-017`, `QUEUE-REQ-018`, `QUEUE-REQ-019`, `QUEUE-REQ-024`, `QUEUE-REQ-025` | Scenarios cover same-message sequential/concurrent delivery; duplicate before effect, after durable effect, and after acknowledgement; different provider receipt; identical/conflicting result duplicate; different result IDs for one attempt; late result after successor attempt; commit then lost ack; crash before commit; stale-fence result; publication uncertainty; producer crash window; consumer restart/durable deduplication. | Cross-component races and crash windows cannot be proven by isolated unit checks. | Weakening duplicate, concurrency, atomicity, fencing, or acknowledgement boundaries is breaking; implementation changes require renewed evidence. | Deterministic race barriers; exact durable commit and acknowledgement-eligibility points; accepted-effect count; conflict classification; no last-write-wins; no duplicate accepted Evidence; durable deduplication across restart; stable publication-intent reconciliation. | All required owner contracts are versioned and the deterministic race, duplicate, crash, and acknowledgement-loss suite passes. | `NO` | `YES` | `YES` |
| `INT-QUEUE-004` — Claim, retry, cancellation, checkpoint and DLQ fault injection | `A2-INTEGRATION` | Sections 6, 8, 9, 10; `QUEUE-REQ-008`, `QUEUE-REQ-009`, `QUEUE-REQ-010`, `QUEUE-REQ-011`, `QUEUE-REQ-014`, `QUEUE-REQ-015`, `QUEUE-REQ-021` | Approved policies/values cover lease duration, visibility timeout, heartbeat/renewal intervals, acknowledgement deadline, retry count, poison bound, backoff, re-drive limit, DLQ retention, authorization freshness, checkpoint compatibility, Evidence deletion/tombstone behavior, cancellation races, and operator replay authorization. | Recovery semantics cross adapter, worker, Workflow, Auth, Security, Evidence, and persistence boundaries. | Any listed policy/value or claim/retry/cancellation/checkpoint/DLQ semantic change reopens Integration review. | Worker crash; forced process kill; network partition; lost renewal request/response; lease expiry during execution; late result after confirmed loss; stale acknowledgement; replacement claim; cancellation at every protected boundary; valid/invalid checkpoint resume; malformed/unauthorized message; retry exhaustion; poison message; operator replay; replay after terminal state/Evidence deletion; `EXECUTING_BUGGY` then `EXECUTING_FIXED` invariance. | All external policies/configuration values are approved and the complete fault-injection suite passes. | `NO` | `YES` | `YES` |
| `INT-QUEUE-005` — Current-main reconciliation | `A2-INTEGRATION` | Current PR #24 branch state; Section 15; `QUEUE-REQ-026` | Before readiness/merge, repeat exact changed-path, contract identifier, 26-row title/status, semantic-matrix, consumer-review preservation, cross-contract dependency, conflict, provider-unselected, DB-003 non-authorization, and secret/forbidden-path validation. | Integration conclusions must reflect the actual merge candidate and current semantic inputs. | Reconciliation changes or newer merged semantics reopen affected Integration/owner review. | Recorded outputs for every required repeated validation against current `origin/main`. | PR #24 is based on or reconciled with current `origin/main`, validation passes, and no newer merged semantic input contradicts final dispositions. | `YES — pre-merge freshness and compatibility requirement` | `NO additional effect` | `NO additional effect` |
| `EVAL-CONSTRAINT-001` — Numerical threshold source | `A2-EVALUATION` | Sections 1.2, 16; `QUEUE-REQ-008`, `QUEUE-REQ-009`, `QUEUE-REQ-010`, `QUEUE-REQ-013`, `QUEUE-REQ-026` | No latency, throughput, concurrency, delivery-reliability, recovery-time, or provider-capacity threshold may be invented. Each requires authoritative product specification, accepted owner contract/policy, or versioned measured baseline with declared sample size/methodology. Historical product targets MUST NOT automatically become Queue thresholds. | Unsupported numbers create ungrounded release claims. | Threshold source/version changes require renewed Evaluation; weakening normative semantics requires affected contract review. | Authoritative specification or accepted owner policy, or versioned raw baseline with sample size, methodology, configuration, and reproducibility evidence. | Evaluation accepts every threshold source, method, and release use. | `NO_NORMATIVE_BLOCK — reopen review on relevant change` | `YES` | `YES` |
| `EVAL-CONSTRAINT-002` — Measurement provenance | `A2-EVALUATION` | Sections 12, 14, 17; `QUEUE-REQ-003` through `QUEUE-REQ-011`; `QUEUE-REQ-013` through `QUEUE-REQ-022`; `QUEUE-REQ-024` through `QUEUE-REQ-026` | Provenance includes exact commit; Queue contract/schema, policy/canonicalization, adapter/provider versions; environment/hardware; workload/concurrency; sample size; raw counts; distributions/percentile method; retry/failure classifications; exclusions/limitations; deterministic seed; fault-injection point; raw artefact references; clean-checkout reproduction. Observability alone is not a release pass; logs/traces/Security events are not semantic completion or Evidence proof. | Metrics without complete provenance cannot support comparison, semantics, or release. | Any relevant implementation, environment, workload, method, policy, or reviewed-version change requires remeasurement/review. | Complete provenance package containing every listed item, raw artefacts/counts, calculations, commands, configuration, and independent clean-checkout rerun. | Independent reproduction succeeds and Evaluation accepts the complete evidence package. | `NO_NORMATIVE_BLOCK — reopen review on relevant change` | `YES` | `YES` |
| `EVAL-CONSTRAINT-003` — No hidden exclusions | `A2-EVALUATION` | `QUEUE-REQ-004`, `QUEUE-REQ-019`, `QUEUE-REQ-021`, `QUEUE-REQ-023`, `QUEUE-REQ-024` | Reports separately record raw occurrence count, typed reason, affected identity, disposition, semantic-denominator exclusion, and reliability/Security/integrity-gate effect. Excluded means inapplicable to semantic-success calculation, not silently removed from reliability evidence. | Silent exclusion inflates success and hides safety/reliability failures. | Classification, denominator, or filtering changes require Evaluation and owning-policy review with restated data. | Raw reports and reconciliation totals containing every required field, including invalid/conflicting/deleted/unauthorized occurrences and exclusions. | Evaluation verifies complete accounting and owner-correct success/reliability classification. | `NO_NORMATIVE_BLOCK — reopen review on relevant change` | `YES` | `YES` |
| `EVAL-CONSTRAINT-004` — Deterministic adapter parity | `A2-EVALUATION` | `QUEUE-REQ-022`, `QUEUE-REQ-026` | Every adapter uses the mandatory common provider-neutral conformance suite with comparable deterministic inputs; provider-specific tests are additive and cannot replace it. | Comparable evidence requires the same semantic workload and oracle. | Adapter/suite/version divergence reopens Evaluation and Integration review. | Common-suite outputs, seed/input controls, provider additions, environment/configuration, parity comparison, and reproducibility evidence. | Evaluation and Integration accept parity for each authorized adapter. | `NO_NORMATIVE_BLOCK — reopen review on relevant change` | `YES` | `YES` |
| `EVAL-CONSTRAINT-005` — Review pinned to exact head | `A2-EVALUATION` | Sections 15, 18, 19 | Original disposition is pinned to commit `ed66cd39e5648c496552a3a160f0a7ef8ba7ba8a`, `CONTRACT-QUEUE-001@1.0.0-draft.1`, blob `81bfc79c7b5c33ff6cfeb6cec47b628a82c0b29b`. Later identity, idempotency, result-acceptance, acknowledgement, retry, cancellation, integrity, retention, compatibility, implementation-evidence, or release-gate assignment changes require renewed A2-EVALUATION review; non-semantic reconciliation still verifies the blob unchanged. | Findings cannot automatically transfer to a changed contract blob or semantic boundary. | Any listed relevant change reopens Evaluation; non-semantic reconciliation requires exact blob verification. | Commit/version/blob hashes, clean diff, changed paths, matrix/version validation, evidence mapping, blob-identity proof for non-semantic reconciliation, and renewed disposition when required. | Evaluation records the renewed disposition or exact unchanged-blob verification for the final reviewed head. | `NO_NORMATIVE_BLOCK — reopen review on relevant change` | `YES` | `YES` |

## External policy, evidence, configuration, compatibility, and gate requests

- External policy: Security/Auth fields, disclosure, trust, integrity,
  canonicalization, keys, identities, authorization freshness, events,
  retention, and administrative boundaries.
- Implementation evidence: future authorized persistence, adapter isolation,
  fencing/races, duplicate/conflict convergence, recovery, cancellation,
  redaction/integrity, replay, deletion, and dead-letter behavior.
- Configuration: provider, counts, backoff, jitter, timeouts, cadence, payload
  bounds, capacity, sampling, alerts, and operational thresholds remain
  unselected.
- Compatibility: affected-owner draft.2 re-review, common conformance,
  mixed-version matrix, rollback, and separately defined canonicalizations.
- Release gates: reproducible capacity, concurrency, throughput, latency,
  reliability, Security, drain, persistence-latency, and recovery measurements.

No request is closed as unconditional acceptance, and no future input is
fabricated by this documentation correction.
